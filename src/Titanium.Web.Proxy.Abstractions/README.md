# Titanium.Web.Proxy.Abstractions

MIT-licensed shared contracts for Titanium Web Proxy: routes, clusters, middleware, Plus/Inspector plugin interfaces.

This package contains **types and interfaces only** (no YAML/JSON parsers).
