namespace Titanium.Inspector.Services;

/// <summary>User choice from the adaptive OS CA trust recovery dialog.</summary>
public enum TrustRecoveryChoice
{
    Cancel = 0,
    Primary = 1,
    Secondary = 2,
}
