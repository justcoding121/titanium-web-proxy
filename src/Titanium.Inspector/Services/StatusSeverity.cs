namespace Titanium.Inspector.Services;

/// <summary>Semantic severity for the main-window status bar (and optional toasts).</summary>
public enum StatusSeverity
{
    Neutral,
    Busy,
    Success,
    Warning,
    Error,
}
